# Config for the service named "skillsphere-backend" — matched by
# spring.application.name, the same convention Eureka registration uses.
#
# Empty for now: RiskScoringService (and the risk.early-window-days override
# this file used to carry as a live proof of centralised config) moved to
# analytics-service — see config-repo/analytics-service.yml for the same
# proof, now on the service that actually owns that property.
